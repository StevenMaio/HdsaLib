[ "$(uname)" = "Darwin" ] && BASE_DIR="$HOME" || BASE_DIR="/Users/joshart"
cache_dir="$BASE_DIR/software/results_cache"

./remove_symbolic_links
mkdir "$cache_dir"

cp -r optimization "$cache_dir"

cp -r fwd_hifi_data_generation "$cache_dir"

cp -r hdsa "$cache_dir"

cp -r hifi_optimization "$cache_dir"

cp -r cvar_obj_compare "$cache_dir"

./execute_clean