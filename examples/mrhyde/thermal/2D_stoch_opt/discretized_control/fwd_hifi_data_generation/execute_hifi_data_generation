# Ensure that the MrHyDE executable has been linked to the current directory under the name "mrhyde"

# Set link to optimization solution
ln -s ../optimization/output_lofi_opt_sample_0.exo .
ln -s ../optimization/sample_set.dat .
python3 generate_hifi_z1.py

# Solve forward problem
./mrhyde 

rm output.exo

# Change file name
for (( i=0; i<50; i++ ))
do
    mv "output_sample_${i}.exo" "output_hifi_sample_${i}.exo"
done